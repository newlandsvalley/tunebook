{
  "private": true,
  "scripts": {
    "build": "spago build",
    "example": "spago bundle --package example --outfile dist/tunebook.js --bundle-type app --platform browser"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "purescript": "^0.15.15",
    "purescript-psa": "^0.5.1",
    "spago": "^0.93.42"
  }
}
